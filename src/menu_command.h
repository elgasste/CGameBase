#if !defined( MENU_COMMAND_H )
#define MENU_COMMAND_H

#include "common.h"

typedef enum gmMenuCommand_t
{
   gmMenuCommand_Quit = 0,
   gmMenuCommand_CloseMenu
}
gmMenuCommand_t;

#endif // MENU_COMMAND_H
